# Onboarding status

One-paragraph purpose: where onboarding got to on the Umbrella Corporation brain. `/onboard`
reads and updates this file, so a run that stops part-way resumes rather than repeats.
It is also the handover note: anyone picking this brain up can see what is grounded,
what was assumed, and what is still missing.

Notation: `✓` done · `△` partial · `✗` not started

## Sources

Not yet reviewed — run `/onboard` to wire these up.

## Docs

- `docs/engagement.md` — ✗ not started
- `docs/stakeholders.md` — ✗ not started
- `docs/product-context.md` — ✗ not started
- `docs/technical/` — ✗ not started
- `docs/meetings.md` — ✗ not started
- Index docs (Notion, Google Drive, Miro, Figma — whichever were scaffolded): each
  `docs/<key>.md` — ✗ not started (seed with the client's actual boards, drives, spaces
  or files as pointers, not their content)

## Waiting on a human

Nothing yet. `/onboard` records here anything it needs a person to fetch — an email
chain, a signed SOW, a deck — so a later run can chase it.
