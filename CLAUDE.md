@AGENTS.md

## Claude Code

The `/onboard` skill (`.claude/skills/onboard/SKILL.md`) is first-run setup for a new
brain — invoke it to connect sources and ground the docs. The `/brain` skill
(`.claude/skills/brain/SKILL.md`) wraps the playbook for day-to-day use — invoke it
for orientation and `/brain sync` for the ingest routine.
